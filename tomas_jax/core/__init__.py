"""Core data structures and configuration.

Modules:
    config     - Global constants, model dimensions, species indices
    state      - TomasState NamedTuple (immutable, JIT-compatible)
    mnfix_jax  - Mass-number drift correction (vectorized JAX)
"""
